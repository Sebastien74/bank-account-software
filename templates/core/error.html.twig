{% trans_default_domain 'core' %}

{% set inAdmin = inAdmin is defined and inAdmin %}

<section id="exception-block" class="min-content-height{% if inAdmin %} my-5{% endif %}">
    <div id="errorStatus">
        <div class="errorStatus">
            <div class="errorStatus-code"></div>
            {% include 'bundles/TwigBundle/Exception/messages.html.twig' %}
        </div>
    </div>
</section>
